{
  "name": "hugo-researcher",
  "version": "0.1.0",
  "description": "Formatting toolchain for the hugo-researcher theme.",
  "private": true,
  "license": "GPL-3.0",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.8.4"
  }
}
